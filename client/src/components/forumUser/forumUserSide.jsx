import { connect } from "react-redux";
import "./forumUserSide.css";

function ForumUser() {
    return (
        <div>
            Ay cabron me equivoque tenia que hacer el perfil del usuario
        </div>
    );
}

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps, {})(ForumUser);
